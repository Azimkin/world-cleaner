# world-cleaner (Python port)

Run from this directory with `uv run python world_cleaner.py ../config.toml`.

The config format is the same as the Rust version. The cache is stored as
`world-cleaner-cache.json` beside the config file.
